redump.org checker written in PHP

Usage:
* Edit `includes/config.php` to contain your database settings
* Copy `examples/PS2.php` to `something.php` and edit for your needs - do this for each set you want to check
